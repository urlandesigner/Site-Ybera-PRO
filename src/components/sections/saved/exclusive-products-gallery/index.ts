/**
 * Bloco “Produtos exclusivos” (copy + 3 fotos salão). Removido da home; use noutra rota:
 *   import { ExclusiveProducts } from "@/components/sections/saved/exclusive-products-gallery";
 */
export { ExclusiveProducts } from "./ExclusiveProducts";
